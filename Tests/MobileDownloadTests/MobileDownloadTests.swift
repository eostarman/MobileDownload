import XCTest
@testable import MobileDownload

final class MobileDownloadTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(MobileDownload().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
